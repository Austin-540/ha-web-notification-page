# HA Web Notification Page

Quickly show a full-screen notification in a web browser. You can specify the parameters `title` and `content` in the URL to show as messages. You can also specify `wakelocklen` to give the number of seconds the browser should show the page for.
